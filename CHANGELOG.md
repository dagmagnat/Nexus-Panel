## 2.5.1 — browser subscriptions and compact client control

- Ссылка `/json/<slug>` открывает фирменный сайт при переходе из браузера, но остаётся исходной JSON-подпиской для VPN-приложений; `?download=1` принудительно скачивает файл.
- Массовая операция выбранных клиентов независимо назначает группу, добавляет цветные метки и при необходимости применяет клиентов к узлу.
- Таблица клиентов получила отдельный столбец группы/меток, единый срок и одинаковую высоту строк без технического `sub_slug`.
- Добавлена кнопка перезапуска helper перенаправления и systemd-наблюдатель, который поднимает остановленный helper.
- Исправлены редактор клиента, диагностика, сводка маршрутизации и компоновка при масштабировании Full HD.

## 2.4.4 — reliable short bootstrap installer

- Добавлен короткий неинтерактивный `bootstrap.sh`: он скачивает и проверяет основной установщик, очищает CRLF, затем возвращает интерактивный ввод через `/dev/tty`.
- Bootstrap принудительно удаляет унаследованный `NEXUS_INSTALLER_LIBRARY_ONLY` и включает точку входа через `NEXUS_INSTALLER_FORCE_RUN`, поэтому установка больше не завершается молча.
- Новая установка и восстановление `agg` используют одну короткую raw-ссылку; длинная цепочка `curl/sed/grep/chmod` скрыта внутри bootstrap.
- Версия проекта и встроенного обновления синхронизирована на 2.4.4.

## 2.4.3 — visible installer menu and self-repairing agg

- Исправлена точная причина зависания после шапки: открытие `/dev/tty` больше не перенаправляет `stderr` всего процесса в `/dev/null`, поэтому меню и приглашения ввода видны.
- Свежий внешний `install.sh` сразу обновляет установленную копию и ярлык без изменения базы, а `repair-shortcut` также заменяет повреждённый локальный установщик.
- Ярлыки `agg` и `agg-<instance>` удаляют CRLF из локального скрипта и при запуске без аргументов явно передают команду `menu`.
- README содержит одну проверяемую команду новой установки и отдельную команду восстановления сломанного `agg`. Версия проекта обновлена до 2.4.3.

## 2.4.2 — reliable installer and compact daily UI

- Однострочная установка очищает CRLF перед запуском, а `.gitattributes` принудительно хранит shell-скрипты с LF. Фоновые этапы получают `/dev/null` вместо терминального stdin и больше не ждут невидимого Enter после проверки Docker.
- Удалена дублирующая кнопка создания клиента в верхней панели; действия, четыре фильтра и обновление каталога выровнены и уплотнены.
- Редактор клиента получил устойчивый скролл, ровную сводку, компактный выбор узлов и согласованные кнопки сохранения, продления и отключения.
- Исправлены QR-заголовки и конфликт класса JSON, карточка клиента, онлайн-список, рейтинг расхода, двухколоночные сроки, форма добавления узла и карточки маршрутизации.
- Обновлён cache-buster единого Spectrum Clear CSS. Версия проекта обновлена до 2.4.2.

## 2.4.1 — cleanup of obsolete release artifacts

- Удалены неиспользуемые каскадные CSS-слои, старые одноразовые apply/rollback-скрипты и промежуточные release notes; интерфейс продолжает использовать единый `public/css/spectrum-clear.css`.
- Удалены дубликат логотипа и тяжёлый промежуточный PNG: основным повторно используемым файлом стал `public/img/nexus-logo-512.png`.
- Рабочие данные, миграции, установщик, тесты и актуальная документация сохранены. Версия проекта обновлена до 2.4.1.

## 2.4.0 — client metadata, per-node quotas and safer administration

- Добавлены группы клиентов и несколько цветных меток с фильтрами, мобильными чипами, переносом и полной резервной копией.
- Лимит и расход считаются отдельно для каждой связи клиент–узел: трафик безлимитного сервера больше не попадает в лимит LTE-сервера. Эти значения показываются в подписке, каталоге, дашборде и карточке онлайн-клиента.
- В редактор узла добавлен режим «Все настройки inbound»: полный JSON выбранного inbound загружается из 3x-ui и применяется вместе с неизвестными/новыми полями; основной режим оставлен компактным.
- Перенаправление остаётся DNAT/SNAT в ядре Linux без WireGuard и пользовательского proxy-процесса; добавлены скорость, байты, пакеты и backend для каждого правила, а также сводка на дашборде.
- Дополнительный secret-key в URL больше не участвует во входе. Любой браузер получает обычную форму логина вместо случайного `Not found`; пароль, CSRF, ограничение попыток, allowed IP и необязательная привязка сессии к IP сохранены.
- `agg` показывает меню до проверки apt/Docker, пустой Enter ничего не запускает, опасные действия требуют явного подтверждения, а ошибки получают русское пояснение и полный журнал.
- Добавлена однострочная команда установки. Версия проекта обновлена до 2.4.0. Nexus Agent остаётся отдельным экспериментальным этапом и не включён в этот релиз.

## stage230-subscription-portal-traffic

- Исправлен `Subscription-Userinfo`: upload/download учитываются и для безлимитных узлов, общий лимит берётся из клиента, а несколько share-links одного узла не удваивают расход.
- Получение статистики 3x-ui продолжает перебор API после HTTP 200 с `success=false` и использует inbound-wide fallback для разных версий панели.
- Основная SUB-ссылка показывает фирменную страницу в браузере, но остаётся обычной подпиской для VPN-приложений; старые URL менять не нужно.
- Добавлены `/hiddify/:slug`, публичная страница со сроком, остатком ГБ, узлами, QR и официальными ссылками Happ, v2RayTun и Hiddify.
- Название VPN и подзаголовок страницы настраиваются в панели. Клиенты, UUID, `sub_slug`, `.env` и `data/` не изменяются.

## stage220-mobile-editor-health

- На мобильном списке нажатие на узел открывает полноценный редактор `/nodes/:id/edit`; настольный master-detail сохранён.
- Карточка клиента открывает настоящую форму редактирования со сроком, лимитами, комментарием и привязками узлов; сводка и действия уплотнены для телефона.
- Дашборд начинается с онлайн-клиентов и окончаний в ближайшие 7 дней, затем показывает компактный контроль узлов и перенаправления.
- Фоновая проверка выполняется раз в минуту и опрашивает узлы строго последовательно, чтобы не создавать одновременную нагрузку на панели.
- Проблемы узлов и перенаправления отображаются заметно и ведут в нужный редактор; типовые сетевые, TLS, DNS, HTTP и конфликтные ошибки объясняются по-русски с технической причиной.
- Строки узлов и карточки клиентов на телефоне уменьшены; данные, `.env`, UUID, `sub_slug` и выданные ссылки не изменяются.

## stage210-spectrum-clear-workflow

- Дашборд сфокусирован на онлайн-клиентах и сроках; выбор периода окончания поддерживает 7, 14 и 30 дней.
- Онлайн-список получил ручное и автоматическое обновление 10/30/60 секунд, компактные строки, флаги активных узлов и сохранение открытого списка при просмотре клиента.
- Каталог клиентов обновляет только признак «в сети» без циклического запроса расхода/скорости; мобильные карточки и массовое удаление стали компактнее.
- Узлы автоматически проверяются раз в минуту, поиск и фильтры выровнены, показатели расхода ГБ удалены из строк и боковой карточки.
- Добавление узла показывает поэтапный процент, прошедшее и примерное оставшееся время; окно остаётся открытым до завершения запроса.
- Маршрутизация и диагностика получили защищённые адаптивные сетки; при нижнем мобильном меню скрыт дублирующий hamburger.
- Клиентская база, UUID, `sub_slug`, ссылки, `.env`, `data/` и параметры установки не изменяются.

## stage203-explicit-agg-menu

- `agg` без аргументов теперь сразу показывает компактное цветное меню управления, не запуская `apt`, Docker или обновление до выбора пользователя.
- У пункта обновления удалено опасное значение по умолчанию `[2]`: пустой Enter ничего не запускает и оставляет пользователя в меню.
- Обновление, изменение параметров, восстановление, переустановка и удаление получили отдельное подтверждение; пустой Enter отменяет операцию.
- Меню выбора экземпляра также переработано, валидирует номер и больше не выбирает первый экземпляр по пустому Enter.
- Явные SSH-команды `agg update`, `agg clients ...` и `agg settings ...` сохранены для автоматизации и переноса.
- Добавлены регрессионные тесты меню, отмены подтверждения, отложенной подготовки сервера и корректного shortcut. Клиенты, UUID, ссылки, `.env` и `data/` не меняются.

## stage202-spectrum-clear-routing-mobile-login

- Маршрутизация переведена на отдельную адаптивную сетку 3:2; вложенные поля больше не наследуют ошибочную 12-колоночную раскладку.
- Критическая сетка маршрутизации продублирована в шаблоне, поэтому старый кеш CSS не может снова сжать форму.
- Ревизия Spectrum CSS и manifest обновлена, для обоих ресурсов установлены заголовки `no-store`.
- PWA запускается через `/mobile-login`; старые `/login` и HTML-переходы на закрытые страницы восстанавливаются в форму входа вместо `Not found`.
- Небраузерные запросы без ключа по-прежнему скрыты, а логин защищён паролем, разрешёнными IP и блокировкой перебора.
- Клиентская база, UUID, `sub_slug`, ссылки, `.env` и `data/` не изменяются.

## stage201-spectrum-clear-ui-correction

- Убран dashboard-блок «Потребление по узлам»; на мобильном dashboard осталось три компактных рабочих вкладки.
- VPN-сервисы удалены из основной/дополнительной навигации, `/vpn` безопасно перенаправляется в клиентов, а каталог клиентов работает только в режиме Xray/Reality.
- Удалён дублирующий Telegram-раздел внутри настроек; отдельная страница Telegram сохранена.
- Исправлена чрезмерная специфичность общего оформления кнопок, из-за которой строки узлов и действия клиентов превращались в фиолетовые блоки.
- Перестроены формы маршрутизации, перенаправления, SNI, переноса настроек, безопасности и обслуживания сервера.
- Добавлены отдельные мобильные вкладки настроек, карточки узлов, drawer деталей, карточки клиентов и адаптивные формы.
- База, `.env`, UUID, `sub_slug`, клиентские ссылки и связи не изменяются; устаревшие VPN-данные остаются в базе для безопасного отката.

## stage200-spectrum-clear

- Полностью заменён каркас интерфейса: новая группированная боковая навигация, единая верхняя панель, компактная рабочая область и мобильная нижняя навигация.
- Создан единый дизайн-слой `public/css/spectrum-clear.css` для тёмной и светлой тем. Исторические `style/redesign/nexus-ui/branding/stage*.css` больше не загружаются ни на одной странице.
- Переработаны все основные сценарии: дашборд, master-detail узлов, каталог и мобильные карточки клиентов, VPN, routing, redirects, diagnostics, Telegram, настройки, вход и публичная страница подключения.
- Убрано конфликтующее поведение нескольких старых UI-контроллеров; порядок меню учитывает новые смысловые группы, а настройки используют общий переключатель вкладок.
- Добавлены `NEXUS_PRODUCT_SPEC.md`, `DATA_COMPATIBILITY.md` и `NODE_ARCHITECTURE.md`, чтобы требования к интерфейсу, клиентским ссылкам и будущей Panel/Node-архитектуре хранились вместе с кодом.
- Добавлены регрессионные проверки единственного CSS-источника, всех экранов, светлой/тёмной темы, мобильных компоновок и HTTP smoke-test 13 реальных маршрутов.
- UI-обновление не содержит миграций БД и не изменяет UUID, `sub_slug`, клиентские связи, `.env` или `data/`. Версия обновлена до Nexus Panel 2.0.0.

## stage113-settings-transfer

- Rev.2 исправляет `allowedColumns.has is not a function` при экспорте двух и более строк inbound-кэша или SNI-профилей и добавляет регрессионный тест этого сценария.
- Добавлен отдельный зашифрованный формат `.nxsettings` для переноса настроек без клиентов: настройки подписок/Happ/routing/Telegram, узлы и inbound-кэш, SNI, редиректы, VPN-хосты и сервисы.
- Пароли и API-токены расшифровываются с `APP_SECRET` старой установки внутри контейнера, защищаются парольной фразой AES-256-GCM и при импорте перешифровываются `APP_SECRET` новой установки.
- Добавлены SSH-команды `agg settings export`, `inspect`, `import --dry-run` и интерфейс на странице настроек. Команды работают и после файлового патча без установленного Node.js на хосте.
- Параметры развёртывания, ключ входа, разрешённые admin IP, администратор и клиенты новой панели не заменяются. Перед настоящим импортом создаётся SQLite-backup; редиректы и VPN-сервисы импортируются отключёнными.
- Добавлен интеграционный тест шифрования пакета, неверной парольной фразы, полного rollback, переназначения ID узлов и перешифрования секретов. Версия обновлена до Nexus Panel 1.0.9.

## stage112-client-transfer

- Добавлен автономный `scripts/client-transfer.py`, который экспортирует клиентов напрямую из SQLite даже при остановленной веб-панели и Docker-стеке.
- UUID, `sub_slug`, сроки, лимиты, комментарии, статистика и локальные связи с узлами переносятся в версионированном JSON с SHA-256-контролем целостности; пароли и API-токены узлов исключены.
- Добавлены SSH-команды `agg clients export`, `agg clients inspect` и `agg clients import`, включая транзакционный `--dry-run`, режимы `skip/update/replace` и `none/match/selected` для связей с узлами.
- На странице клиентов появилась форма «Перенос клиентов» с экспортом, предварительной проверкой конфликтов и импортом. Веб и CLI используют одну реализацию.
- Настоящий импорт сначала создаёт SQLite-backup, не обращается к удалённым узлам и сохраняет старые подписочные пути при переносе прежнего домена на новый IP.
- Добавлены интеграционные тесты прямого экспорта, отката dry-run, сохранения UUID/sub_slug, смены локального node ID и защиты от конфликта идентичности. Версия обновлена до Nexus Panel 1.0.8.

## stage111-safe-legacy-update

- Исправлена миграция существующей установки с `dagmagnat/3xui-Aggregator`: updater загружает выбранный URL напрямую и после успеха меняет старый Git `origin` на `dagmagnat/Nexus-Panel`.
- Git запускается с отключёнными интерактивными credential-запросами, поэтому обновление больше не зависает на `Username for 'https://github.com':`.
- Репозиторий теперь скачивается и проверяется до остановки Docker-стека. При 404, пустом или закрытом источнике работающая панель и её файлы остаются нетронутыми.
- При успешном переходе сохраняются `data/` с `app.db`, `.env`, `.install.conf` и `.source.conf`; полная переустановка для миграции не требуется.
- Добавлены shell-интеграционные тесты переноса старого официального URL, смены origin с сохранением runtime-данных и отказоустойчивости недоступного источника. Версия обновлена до Nexus Panel 1.0.7.

## stage110-nexus-branding

- Создан оригинальный знак Nexus Panel: геометрическая N из сетевых маршрутов внутри защищённого шестигранного контура.
- Логотип добавлен в сайдбар, мобильную шапку, страницу входа и публичную страницу подключения клиента.
- Добавлены многоразмерный `favicon.ico`, Apple Touch Icon, PNG 192/512 px и `site.webmanifest`.
- Публичная страница клиента получила Open Graph-метаданные; JSON-подписка содержит `logoUrl`, `iconUrl` и фирменный web URL.
- Официальный заголовок Happ `profile-web-page-url` теперь ведёт на `/open/:slug`, где используется тот же логотип и favicon; ссылка поддержки остаётся отдельной.
- `package-lock.json` больше не исключается из Git, поэтому воспроизводимая установка через `npm ci` сохраняется в репозитории.
- Добавлены документация фирменных файлов и автоматические проверки размеров/подключения иконок. Версия обновлена до Nexus Panel 1.0.6.

## stage109-nexus-official-repository

- Репозиторий установки, встроенного обновления и формы настроек изменён на `https://github.com/dagmagnat/Nexus-Panel`.
- Добавлена миграция старого адреса `dagmagnat/3xui-Aggregator` в SQLite и `.source.conf`; пользовательские fork остаются без изменений.
- README переписан под Nexus Panel: актуальная быстрая установка, обновление через `agg update`, требования к портам и корректная ручная установка без несуществующего `.env.example`.
- В `package.json` добавлены официальный repository, homepage и адрес issues; релиз обновлён до Nexus Panel 1.0.5.
- Добавлен тест: новый адрес используется по умолчанию, старый официальный URL переносится, пользовательский fork сохраняется.

## stage108-inbound-auto-sync-traffic

- При добавлении обычного 3x-ui узла полный выбранный `Inbound ID` загружается и проверяется до записи узла в базу; неверный ID, токен или неполный ответ больше не оставляют полупустой offline-узел.
- При изменении подключения/Inbound ID новая конфигурация также проходит preflight, поэтому рабочие параметры не заменяются данными другого или отсутствующего inbound.
- Удалено использование `/panel/api/inbounds/list/slim` как источника конфигурации: сокращённый ответ без `streamSettings` больше не попадает в кэш.
- Редактор автоматически синхронизирует выбранный inbound и показывает только относящиеся к нему поля транспорта и security: например, XHTTP/NONE не показывает Reality, gRPC и mKCP.
- В списке и деталях узла показываются `up + down`, загрузка и отдача именно выбранного inbound; на старых fork без агрегатных полей используется локальная сумма связей только этого узла.
- Добавлен интеграционный тест полного сценария: exact Inbound ID, VLESS/XHTTP/NONE/packet-up, изоляция полей, 3 ГБ upload + 7 ГБ download и отказ от сохранения отсутствующего ID.

## stage107-bulk-remove-node-502

- Исправлен HTTP 502 при операции «Удалить выбранных клиентов из узла»: вместо двух полных сканирований списка 3x-ui на каждого клиента используется штатный `POST /panel/api/clients/bulkDetach`.
- Большие выборки отправляются линейными пакетами по 200 email; ответы `detached`, `skipped` и «клиент уже отсутствует» безопасно синхронизируют локальные связи.
- Для старых и нестандартных версий 3x-ui сохранён совместимый поштучный fallback, который больше не загружает глобальный список клиентов до и после каждого удаления.
- Длительные POST-операции отключают буферизацию Nginx и сразу отправляют страницу прогресса с heartbeat.
- Добавлен интеграционный тест массового удаления 225 клиентов: два API-запроса, отсутствие list-сканирований, сохранение клиентов агрегатора и работающий процесс Node.

## stage106-3xui-xhttp-external-proxy

- Исправлена генерация SUB/JSON для обычных узлов 3x-ui с XHTTP External Proxy: клиентский адрес, порт и Force TLS теперь берутся из `streamSettings.externalProxy`, а не из URL админ-панели и backend-порта inbound.
- Поддержаны актуальные режимы 3x-ui `forceTls= same | tls | none`; явный `none` больше не заменяется безопасностью исходного inbound.
- Для прямых ссылок учитываются штатные `shareAddrStrategy` и `shareAddr` 3x-ui.
- JSON обычного 3x-ui сохраняет `xPaddingObfsMode` и остальные XHTTP `extra`, чтобы параметры клиента совпадали с рабочим экспортом оригинальной панели.
- Добавлен интеграционный тест сценария `pnl220:2053/none → la57l:443/tls`.

## stage105-nexus-login-key-recovery

- Исправлен ответ `Not found` для ссылки `/login?key=...`, если ключ в `.env` разошёлся с зашифрованным `panel_access_key` в SQLite после восстановления базы или изменения конфигурации.
- Актуальный ключ установки действует как одноразовый ключ восстановления и после успешной проверки синхронизируется с SQLite.
- Ключ, вручную заданный в авторизованных настройках панели, остаётся главным: старое значение из `.env` не превращается в постоянный запасной вход.
- Установщик и режим диагностики теперь печатают фактический runtime-ключ из SQLite, с fallback на `.env` только когда база недоступна.
- Добавлен интеграционный тест восстановления, ручной смены и последующей ротации ключа.

## stage103-horizontal-qr-silent-copy

- QR-коды SUB и JSON теперь показываются горизонтально в двух аккуратных карточках с одинаковым размером.
- После копирования кнопка больше не превращается в надпись «Скопировано»: вместо этого она кратко подсвечивается.
- В быстрой информации клиента добавлены отдельные кнопки «Копировать SUB» и «Копировать JSON».
- Кнопка «Все ссылки и QR-коды» открывает полный выбор форматов без потери прежних возможностей.

## stage102-mobile-sub-h1cloud-cleanup

- Исправлен мобильный импорт Happ: `/json/:slug` больше не возвращает HTML по Safari/Chrome User-Agent.
- JSON QR использует `?raw=1`, SUB QR использует чистый `/sub-plain/:slug`.
- QR-коды переведены с SVG на PNG и адаптированы под экран телефона.
- H1Cloud удалён из интерфейса; старые совместимые записи автоматически мигрируют в обычный 3x-ui.

## Stage101 — Удаление H1Cloud и переход на обычный 3x-ui

- Из интерфейса добавления, редактирования, фильтров и карточек узлов удалены отдельные типы `H1Cloud 3x-ui` и `H1Cloud VLESS API`.
- Все новые и редактируемые панели теперь используют обычный тип `3xui`; Remnawave остаётся отдельным поддерживаемым типом.
- Существующие записи `h1cloud_3xui` автоматически мигрируют в `3xui` без отключения и сохраняют адрес, авторизацию, Panel Path и Inbound ID.
- Несовместимые записи старого `h1cloud` API автоматически переводятся в отключённый черновик `3xui`; администратору нужно указать параметры стандартной панели и включить узел вручную.
- Удалены H1Cloud-поля и подсказки из пользовательских EJS-шаблонов, установщика, README и списка документации.
- Устаревшие столбцы и внутренний код чтения сохранены временно только ради безопасной миграции существующих SQLite-баз; создать или выбрать H1Cloud через UI больше нельзя.

## Stage90 — Безопасность и live-разработка интерфейса

- Добавлен изолированный development-режим: `npm run dev` создаёт отдельные `.env.development` и `data-dev/`, перезапускает backend через nodemon и поднимает BrowserSync на `http://localhost:3001`.
- В development-режиме появился визуальный **UI inspector**: клик по элементу выдаёт URL, предполагаемый EJS-шаблон, CSS-селектор и готовый формат задачи.
- Добавлены `docs/DEVELOPMENT.md`, `docs/UI_MAP.md` и отдельный последний слой переопределений `public/css/stage90.css`; удалены корневые дубли EJS/CSS и архивные `.bak`, чтобы место изменения UI определялось однозначно.
- Production-запуск теперь блокируется при стандартных/слишком коротких `APP_SECRET`, `SESSION_SECRET` и пароле `admin`; `.env` получает права `0600`.
- Добавлена единая CSRF-защита форм и same-origin `fetch`, security headers, скрытие stack trace в production и удаление ключа доступа из URL после подтверждения.
- Пользовательский флаг проходит серверную Unicode-валидацию; закрыт stored XSS через raw HTML во флагах.
- Telegram-токены и proxy URL шифруются в `app_settings` с прозрачной миграцией ранее сохранённых plaintext-значений.
- Включена проверка TLS H1Cloud по умолчанию для новых установок; старые установки сохраняют совместимость и получают предупреждение при отключённой проверке.
- Добавлены `package-lock.json`, `npm ci`, Node.js 22 LTS и статические проверки backend, browser JS и EJS. Удалена неиспользуемая зависимость `3xui-api-client`.

## Stage89 — Настоящие флаги на Windows и починка мобильных настроек

- Флаги стран теперь видны в Windows. Причина была не в коде: Windows принципиально не содержит символов флагов и подставляет вместо них две буквы (SE, NL, DE). В проект встроен шрифт Twemoji Country Flags (77 КБ, лицензия MIT, `public/fonts/`), подключённый через `unicode-range` — он влияет только на символы флагов и никак не меняет остальной текст. На телефонах флаги отображались и раньше.
- Исправлена мобильная шапка: у `.mobile-shellbar` стоял `display:block` вместо строки, из-за чего кнопка переключения темы выпадала из шапки и висела поверх содержимого страницы.
- Починены мобильные настройки. Скрипт сворачивает длинные разделы в аккордеон на любой теме, но все стили аккордеона были написаны только для темы `mobile_lite`. На стандартной теме разделы оставались без оформления — отсюда пустые блоки и наезжающие друг на друга элементы. Стили аккордеона добавлены для всех тем и оформлены аккуратными карточками с акцентной полосой и подписью «Открыть/Скрыть».
- Формы настроек больше не сжимаются на телефоне: у блока Happ Control App справа была жёстко закреплена колонка помощи шириной 300px, из-за чего форме оставалась узкая полоска и подписи переносились по одному слову. На мобильном раскладка теперь одноколоночная, поля тянутся на всю ширину, панель помощи уходит вниз.

## Stage88 — Фикс зависимостей, флаги в состоянии узлов, честная сортировка сроков

- Зафиксированы точные версии нативных зависимостей: `connect-sqlite3` 0.9.15 и `better-sqlite3` 11.10.0 (убрана «крышка» `^`). Это устраняет причину ошибки 502 при пересборке образа: раньше npm без lock-файла подтягивал несовместимую свежую версию `connect-sqlite3`, и хранилище сессий падало с `this.db.exec is not a function`.
- Строка «Состояние узлов» на дашборде теперь показывает флаг страны (эмодзи), как и в остальных списках узлов; если у узла нет кода страны, показывается прежний текстовый бейдж (для мобильных/БПЛА-узлов без страны это нормально).
- Исправлена сортировка блока «Сроки»: режимы «Сначала меньше дней», «Сначала больше дней» и «Сначала истёкшие» теперь работают по-разному и предсказуемо. Раньше все истёкшие клиенты имели одинаковое значение «0 дней», из-за чего «меньше дней» и «истёкшие» вели себя одинаково. Добавлен точный расчёт дней со знаком (просрочка считается в минус), истёкшие больше не скрываются, а корректно раскладываются по выбранному режиму.

## Stage87 — Полировка интерфейса, светлая тема и мобильная версия

- Полностью перерисована светлая тема: мягкий фон `#f3f5f9`, белые карточки с чёткими границами, читаемый тёмный текст, единый синий акцент `#2e63e8`; убраны «недокрашенные» тёмные элементы (поля ввода, code/pre, чипы).
- Единый вид модальных окон: выравнивание по левому краю, шапка с заголовком, аккуратный крестик 34×34 в углу, прокрутка внутри карточки; на телефоне окна открываются «шторкой» снизу с ручкой и отступом под безопасную зону iPhone.
- Все эмодзи в навигации заменены на единые контурные SVG-иконки: сайдбар, нижнее меню, бренд, переключатель темы, вкладки дашборда и карточки «Ещё».
- Полосы-алерты сверху страницы превращены во всплывающие уведомления (тосты) с автоскрытием и кнопкой закрытия.
- Мобильные исправления: поля ввода 16px (iPhone больше не приближает страницу при тапе), кнопки не ниже 42px, таблицы прокручиваются по горизонтали, плашка «Есть изменения» располагается строго над нижним меню, нижняя навигация крупнее и с иконками.
- Дашборд: новая строка «Состояние узлов» (флаг, имя, точка статуса, время отклика в мс по кнопке «Проверить») и график скачанного трафика по дням за 30 дней в карточке «Сколько было скачано» (новый маршрут `/dashboard/traffic-series.json`).
- `/dashboard/panel-check.json` теперь возвращает `nodeId` и время ответа каждого узла в `details`.
- Стили вынесены в новый файл `public/css/stage87.css?v=1`, подключён после `style.css` и `redesign.css`; после обновления сделайте жёсткое обновление страницы (Ctrl+F5), чтобы сбросить кеш.

## Stage86 — H1Cloud protected read-only

- Для узлов **H1Cloud 3x-ui** добавлена жёсткая серверная блокировка любых попыток изменить provider-managed inbound: порт, протокол, XHTTP/Reality, TLS, SNI, External Proxy, fingerprint, flow, sniffing и остальные параметры.
- Полное обновление клиента H1Cloud через общий API заблокировано; используется отдельный защищённый updater с белым списком клиентских полей.
- Существующие H1Cloud-клиенты больше не перезаписываются при проверке узла, импорте, синхронизации, создании связей или запуске агрегатора.
- Удалена стартовая процедура массовой пересинхронизации квот H1Cloud.
- Явное редактирование клиента может изменить только имя/login, ГБ, срок, IP-лимит, статус и комментарий; flow, subId, ключи и прочие параметры сохраняются из удалённой панели.
- При переименовании сначала ищется прежний remote email, поэтому агрегатор не создаёт дубль клиента.
- Если клиент существует, но ещё не привязан к выбранному inbound, выполняется только привязка без перезаписи его параметров.
- При создании нового клиента flow берётся только из готового inbound H1Cloud; если у провайдера flow отсутствует, агрегатор не добавляет его самостоятельно.
- Локальный fallback подписки больше не подставляет выдуманные `fp=chrome` и `spx=/` для H1Cloud, если этих значений нет в исходном inbound.

## Stage85 — Safe deletion, two themes, dirty state and online clients

- Удаление клиента больше не блокируется отсутствующими, отключёнными или недоступными узлами: удалённый запрос для них пропускается, а устаревшая локальная связь очищается.
- Для доступных узлов выполняется реальное удаление клиента; ответ «клиент не найден» считается успешным результатом и также очищает локальную связь.
- При массовом удалении доступность каждого узла проверяется один раз и повторно используется для всех выбранных клиентов, поэтому выключенный VPS не создаёт отдельный тайм-аут для каждого клиента.
- Удаление выбранных клиентов и удаление узла у выбранных используют обычное подтверждение «Да/Нет».
- Только опасная операция «Удалить всех клиентов» открывает собственное модальное окно панели и требует вручную ввести `YES`; сервер дополнительно проверяет подтверждение.
- Переключатель темы оставляет только два режима: тёмный и светлый. Светлая палитра основана на токенах официального 3x-ui/Ant Design, но фон и поверхности смягчены, чтобы интерфейс не был ослепительно белым.
- Панель «Есть несохранённые изменения» теперь принимает только прямой ввод пользователя в конкретном поле. Переключение вкладок, клики по пустому месту и программные события её не открывают.
- Окно онлайн-клиентов перестроено: имя, комментарий, срок, «Подробнее» и продление помещаются внутри одной карточки без выхода за границы; на узких экранах продление переносится отдельной строкой.
- Версия CSS-кеша повышена до 185.

## Stage84 — Filter, light theme, timeouts and online modal

- Исправлен фильтр Reality/Xray-клиентов по узлу: скрываются все карточки, не связанные с выбранным узлом, во всех адаптивных вариантах списка.
- «Выбрать все» и счётчик теперь работают только с реально показанными после фильтра клиентами.
- Массовое удаление клиентов и массовое удаление узла требуют ручного ввода `YES`; подтверждение показывает узел, режим и список выбранных клиентов.
- Сервер дополнительно проверяет подтверждение `YES`, поэтому опасное действие нельзя случайно отправить обходным запросом.
- При удалении с недоступного узла используется короткий тайм-аут; локальные клиенты и связи сохраняются для повторной попытки.
- Удаление одного клиента с нескольких узлов выполняется параллельно, поэтому один выключенный VPS больше не задерживает обработку остальных узлов последовательно.
- Ручная проверка узла ограничена отдельным коротким тайм-аутом `NODE_HEALTHCHECK_TIMEOUT_MS` (по умолчанию 7 секунд).
- Светлая тема переработана по цветовой системе официального 3x-ui/Ant Design: серый фон страницы, белые карточки и боковая панель, тёмный текст, синий активный пункт и корректный контраст кнопок.
- Окно онлайн-клиентов расширено; карточки получили нормальную высоту, перенос длинных имён, полностью видимую кнопку «Подробнее» и запас прокрутки после последней записи.
- Версия CSS-кеша повышена до 184.

## Stage83 — Client node filters, themes and dashboard search

- Поиск Reality/Xray-клиентов переведён на локальную фильтрацию без перезагрузки страницы: поле остаётся активным при наборе текста.
- Добавлен фильтр по узлу: после выбора узла показываются только клиенты, реально связанные с ним; последующий поиск выполняется внутри выбранного узла.
- Добавлена сортировка клиентов: последние добавленные, старые сначала, А–Я и Я–А.
- «Выбрать все» теперь выбирает только клиентов, показанных текущим поиском и фильтром по узлу.
- Добавлен переключатель темы с режимами «автоматически», «светлая» и «тёмная»; выбор сохраняется в браузере, автоматический режим следует системной теме.
- Добавлен светлый вариант интерфейса без изменения существующей тёмной темы.
- В разделе сроков и в окне лимитов добавлен мгновенный поиск по имени, логину, ID и узлу.
- Исправлены модальные окна дашборда: красная кнопка закрытия остаётся доступной при прокрутке, а список онлайн-клиентов прокручивается до последней карточки.
- Версия CSS-кеша повышена до 183.

## Stage82 — Clients select all and node removal

- В разделе **Клиенты → Reality / Xray** добавлены кнопки «Выбрать все» и «Снять выбор» с точным счётчиком выбранных клиентов.
- Выбор синхронизируется между мобильным, компактным и табличным представлением одного и того же клиента, поэтому ID не дублируются при массовых действиях.
- Добавлено массовое действие «Убрать узел у выбранных»: клиент удаляется с выбранного 3x-ui/H1Cloud-узла, а локальная связь `client_nodes` удаляется только после успешного удаления на сервере.
- Сам клиент остаётся в агрегаторе и на остальных узлах; при ошибке удалённая связь сохраняется для повторной попытки.
- «Выбрать все» действует на клиентов, отображаемых текущим поиском.
- Массовое удаление клиентов теперь также устраняет повторную обработку одинаковых ID из адаптивной вёрстки.
- Добавлены отдельные подтверждения и тексты прогресса для удаления клиентов и удаления одного узла у выбранных клиентов.
- Версия CSS-кеша повышена до 181.

## Stage81 — VPN local host gateway fix

- Исправлена ошибка `getaddrinfo ENOTFOUND host.docker.internal` при проверке локального сервера агрегатора.
- Все варианты `docker-compose.yml`, создаваемые установщиком, теперь добавляют `host.docker.internal:host-gateway` для контейнера агрегатора.
- Исправление применяется не только к шаблонному compose-файлу в архиве, но и к реально генерируемым конфигурациям: IP, домен, домен с портом и смешанный режим.
- После обновления контейнер пересоздаётся и получает запись хоста для подключения к служебному SSH на сервере агрегатора.

## Stage80 — VPN local sudoers path fix

- Исправлена остановка обновления на создании `/etc/sudoers.d/3xui-aggregator-vpn`.
- Установщик больше не записывает shell builtin `true` в sudoers: пути к `bash` и `true` определяются только как абсолютные исполняемые файлы.
- Проверка passwordless sudo в VPN-менеджере использует тот же абсолютный путь `/usr/bin/true` с fallback на `/bin/true`.
- Правило проверено через `visudo -cf`; обновление больше не должно оставлять стек остановленным из-за этой ошибки.

## Stage79 — VPN SSH auth mode fix

- Поля авторизации стороннего VPS теперь строго взаимоисключающие.
- В режиме «Логин и пароль» показывается только SSH-пароль; приватный ключ и passphrase скрыты, отключены и не отправляются.
- В режиме «Приватный SSH-ключ» SSH-пароль скрыт, отключён и не отправляется; остаются пользователь, ключ и необязательный passphrase.
- Добавлена защита CSS и JavaScript от повторного появления скрытых полей из-за правил макета VPN-формы.

# stage78-vpn-clients-domain-migration-ui-fix — 2026-06-27

- Исправлена плавающая панель «Есть несохранённые изменения»: она появляется только после реального изменения значения поля пользователем и больше не реагирует на обычные клики, вкладки или программное автозаполнение.
- Полностью переработана компоновка раздела **VPN-сервисы**: тёмные читаемые вкладки, ровная форма VPS, отдельные карточки подключённых серверов и адаптивная вёрстка.
- Для сервера агрегатора больше не запрашиваются SSH-логин и пароль: установщик создаёт отдельного пользователя `aggvpn`, локальный Ed25519-ключ и ограниченное sudo-правило для служебных VPN-скриптов.
- Для стороннего VPS сохранён выбор входа по паролю или приватному SSH-ключу с passphrase.
- В общем разделе **Клиенты** добавлены отдельные вкладки Reality/Xray, WireGuard, AmneziaWG и Outline. Создание, поиск, QR-код, скачивание, переименование, продление, включение и удаление выполняются в выбранной вкладке.
- На уровне каждого VPN-сервиса добавлены редактируемые общие параметры: имя, клиентский домен/IP, порт, DNS и MTU. Новые конфигурации создаются автоматически, существующие WG/AWG-конфигурации перегенерируются в агрегаторе.
- При смене порта WireGuard/AmneziaWG старый UDP-порт сохраняется через совместимое перенаправление, чтобы ранее выданные конфигурации продолжали работать после изменения. Возврат на прежний порт безопасно убирает старое перенаправление, а при переносе сервиса совместимые порты восстанавливаются на новом VPS.
- Добавлена DNS-проверка клиентского домена против публичного адреса VPS с зелёным/красным статусом и отображением разрешённых IP.
- Добавлен перенос установленного WireGuard, AmneziaWG или Outline на другой VPS. Агрегатор устанавливает сервис на новом сервере и восстанавливает клиентов из сохранённых ключей и данных; для туннельных протоколов сохраняются серверный и клиентские ключи.
- Перед переносом проверяются конфликты порта, имени интерфейса и VPN-подсети на целевом VPS.
- Для Docker добавлен `host.docker.internal:host-gateway`, а установщик автоматически подготавливает безопасный локальный SSH-контур.
- Версия CSS-кеша повышена до 179.

# stage77-vpn-services-ssh — 2026-06-27

- Добавлен отдельный раздел VPN-сервисов, не смешанный с узлами и клиентами 3x-ui/Xray.
- Поддержано подключение к серверу агрегатора или стороннему VPS по SSH-паролю либо приватному ключу с passphrase.
- После первой проверки сохраняется SSH host fingerprint; последующие подключения отклоняются при его изменении.
- Реализована автоматическая установка и управление системным WireGuard, AmneziaWG Legacy, AmneziaWG 2.0 и Outline.
- Для WireGuard/AmneziaWG создаются отдельные интерфейсы, подсети, UDP-порты, серверные ключи и peer-конфигурации.
- Для Outline используется Management API с проверкой SHA-256 fingerprint TLS-сертификата.
- Добавлены VPN-клиенты: создание, привязка к существующему клиенту агрегатора, QR-код, скачивание конфигурации, включение, отключение, продление и удаление.
- Срок доступа при создании: 0 — бессрочно, иначе 1–3650 дней; продление — минимум 30 дней. Истёкшие конфигурации автоматически отключаются и включаются после продления.
- Учетные данные, приватные ключи, клиентские конфигурации и резервные копии сохраняются в базе агрегатора в зашифрованном виде.
- После изменения peer/ключей обновляется резервная копия сервиса на агрегаторе; рабочая конфигурация остается на VPS.

# stage76-3xui-v340-timeout-api-fix — 2026-06-25

- Совместимость с 3x-ui v3.4.0: авторизация строго через `Authorization: Bearer`, без неофициальных `X-API-*` заголовков.
- Исправлена ошибка `The user aborted a request`: панельные API-запросы получили отдельный тайм-аут 30 секунд и понятную диагностику URL/этапа.
- Проверка узла больше не загружает тяжёлый `/inbounds/list`; используется точечный `/inbounds/get/:id`, а для старых панелей — `/list/slim` и только затем legacy list.
- Удаление клиента не влияет на проверку узла: проверяются статус панели, токен, Panel Path и конкретный Inbound ID.
- Массовое создание существующих клиентов на новом узле выключено по умолчанию и запускается только по явной галочке.
- Добавлена поддержка XHTTP `sessionIDPlacement`, `sessionIDKey`, `sessionIDTable`, `sessionIDLength` из Xray v26.6.22 / 3x-ui v3.4.0.
- В сообщении успешной проверки показывается версия удалённой 3x-ui, если она присутствует в ответе API.

# stage75-h1cloud-quota-sub-first-json-merge-ui-fix — 2026-06-22

- Исправлена модель лимитов H1Cloud 3x-ui: назначенный клиенту лимит ГБ всегда записывается в удалённую 3x-ui как `totalGB`; значение `0` означает безлимит только когда администратор сам назначил 0 ГБ.
- Все существующие узлы H1Cloud 3x-ui мигрируют из ошибочного режима `shared_traffic=1` в режим явных клиентских квот. После первого запуска stage75 агрегатор один раз синхронизирует уже назначенные квоты с удалённой панелью.
- Рабочая провайдерская `/sub/<subId>` ссылка теперь является источником истины, имеет приоритет над API share links и объединяется с остальными узлами общей подписки.
- Общий `/json/<slug>` объединяет H1Cloud-конфигурацию с основными узлами. Можно указать JSON-порт, Base URL или полный шаблон с `{subId}`; без отдельного JSON endpoint конфигурация строится из рабочей SUB-ссылки с сохранением XHTTP `extra`.
- Блок H1Cloud 3x-ui в формах добавления и редактирования полностью растянут на ширину формы; добавлен независимый CSS hard-fix для desktop/mobile.
- Флаги во всех основных списках выводятся как emoji и больше не зависят от SVG-файлов.
- Версия CSS-кеша повышена до 176.

# stage73-h1cloud-3xui-shared-pool-external-proxy-fix — 2026-06-22

- Добавлен отдельный тип узла **H1Cloud 3x-ui (панель провайдера, только клиенты)**. Он не смешивается ни с обычной 3x-ui, ни с отдельным H1Cloud VLESS API (`vpn main.sh`).
- Провайдерский inbound сделан строго read-only: агрегатор не меняет порт, XHTTP, TLS, External Proxy, sniffing и остальные параметры, а управляет только глобальными клиентами и их привязкой к выбранному inbound.
- Реализована работа с новым глобальным Clients API 3x-ui v3: создание через `{ client, inboundIds }`, обновление прямым объектом клиента, attach/detach и безопасное удаление глобальной записи.
- Исправлена ошибка `record not found`: она больше не считается отсутствующим API endpoint и не запускает legacy fallback, который раньше мог добавить клиента только в `settings.clients` и оставить осиротевшую запись.
- Исправлено различие между числовым ID записи клиента и UUID подключения в 3x-ui v3. Для VLESS используется поле UUID, а числовой ID хранится отдельно.
- Добавлена диагностика дублей/осиротевших записей: агрегатор останавливает операцию и просит очистить конфликтующий логин в оригинальной H1Cloud 3x-ui вместо создания нового дубля.
- Устаревшее поведение stage73: общий пакет ошибочно превращал клиента в безлимитного. В stage75 это отменено: назначенная квота всегда записывается в удалённую 3x-ui.
- Поддержан локальный срок: удалённая дата окончания может оставаться безлимитной, а агрегатор отключает клиента по собственному сроку. При добавлении уже действующего клиента сохраняется оставшийся локальный срок, а не выдаются новые 30 дней.
- Добавлены порт подписки H1Cloud 3x-ui (по умолчанию `25555`) и отдельный SUB Base URL.
- Для подписки агрегатор сначала запрашивает точные share links через авторизованный API панели (`clients/links` / `clients/subLinks`), затем пробует SUB-порт и только после этого строит локальный fallback из read-only inbound.
- В fallback корректно используется `externalProxy` провайдера: публичный CDN host/port, TLS/SNI, XHTTP path/mode и совместимый `extra`; backend-порт inbound не публикуется как клиентский адрес.
- Исправлена передача API Token в фоновой сборке подписок H1Cloud 3x-ui.
- Обновлены формы добавления/редактирования узла, README и инструкция H1Cloud.

# stage72-node-type-h1cloud-3xui-separation

- Чётко разделены два режима: обычная панель 3x-ui (включая 3x-ui, установленную H1Cloud) и отдельный H1Cloud VLESS API-скрипт с командами `vpn api` / `vpn sub`.
- H1Cloud-поля принудительно скрываются и отключаются при выборе 3x-ui; 3x-ui-поля так же скрываются при выборе H1Cloud. Исправление не зависит от кеша и конфликтующих CSS `display: flex/grid !important`.
- Подписи URL и API Token динамически меняются под выбранный тип узла.
- При смене типа существующего узла старый токен другой системы больше не переиспользуется: панель требует новый токен.
- Добавлены подсказки о том, что панель 3x-ui на сервере H1Cloud всё равно добавляется как 3x-ui.

# stage71-mobile-node-dashboard-routing-fix — 2026-06-20

- На мобильной странице «Узлы» добавлена отдельная панель «Список / Добавить узел / Подсказка», поэтому создание узла доступно без компьютерной версии.
- Исправлен двойной значок сброса фильтров на странице узлов.
- Исправлена мобильная карточка клиента на главной: окно теперь прокручивается по вертикали, не упирается в нижнюю навигацию и поддерживает нормальный жест прокрутки.
- В карточку клиента добавлены быстрые действия «Перейти к клиенту» и «Продлить» с выбором количества дней.
- Блокировка рекламы выключена по умолчанию. При обновлении старый стандартный набор `geosite:category-ads-all` автоматически отключается, если пользователь не добавлял собственные правила.
- DNS по умолчанию изменён на Cloudflare (`1.1.1.1` и `1.0.0.1`); сохранённый пользовательский выбор не перезаписывается.
- Поля IP/GeoIP теперь принимают `ip:157.240.225.60/32` и автоматически сохраняют его как обычный CIDR. Ошибочная форма `geoip:157.240.225.60/32` также нормализуется в CIDR.
- Обновлён cache-bust CSS, чтобы мобильный браузер не использовал старые стили.

# stage70-h1cloud-json-xhttp-cdn-fix — 2026-06-18

- H1Cloud снова включён в общую JSON-подписку `/json/:slug` вместе с 3x-ui узлами; исключение сохранено только для routing-правил H1Cloud.
- В общей `/sub/:slug` и `/happ/:slug` H1Cloud остаётся обычными VLESS-ссылками из `/sub/UUID/local`, а не отдельной подпиской.
- Исправлена конвертация H1Cloud XHTTP в Xray JSON: полностью сохраняется `extra`, старые `sessionPlacement/sessionKey` переводятся в актуальные `sessionIDPlacement/sessionIDKey`, сохраняются GET, header/query placement, padding и XMUX.
- Исправлен XHTTP CDN в JSON: добавлены `tlsSettings` с SNI, fingerprint и ALPN, поэтому CDN больше не превращается в XHTTP без TLS-параметров.
- Для Happ-сборок, которые ошибочно превращают `xPaddingObfsMode=true` в неподдерживаемый FinalMask `noise`, в совместимой SUB/JSON отключается только этот необязательный переключатель; UUID, адрес, порт, path, mode, Host/SNI, TLS и остальные XHTTP-параметры сохраняются.
- Reality оставлен без изменений и сохраняет `xtls-rprx-vision`, public key, SNI, short ID и spiderX.
- Выбор Reality/XHTTP/XHTTP CDN и fingerprint в H1Cloud-узле сохранён.
- В карточке узла кнопки «Проверить», «Изменить» и «Отключить/Включить» получили одинаковые размеры и выравнивание; в редакторе подровнена кнопка «Удалить узел».
- Cache-bust стилей обновлён до `v=170`.

# stage69-h1cloud-merged-sub-xhttp-compat — 2026-06-18

- H1Cloud возвращён в общую `/sub/:slug` и `/happ/:slug` вместе с обычными 3x-ui узлами.
- Для агрегации снова используется H1Cloud `/sub/UUID/local`, чтобы получать ссылки только выбранного H1Cloud-узла.
- В формах добавления и редактирования H1Cloud восстановлен выбор Reality, XHTTP, XHTTP CDN и uTLS fingerprint.
- Исправлена ошибка старых Xray-клиентов `failed to build mask with type noise`: у XHTTP удаляется только необязательный FinalMask `fm/finalMask`; остальные параметры ссылки сохраняются.
- H1Cloud по-прежнему исключён из JSON-маршрутизации.
- Кнопки массового удаления клиентов выровнены относительно списков режима удаления.
- В редакторе клиента для H1Cloud добавлены компактные SUB/QR рядом с JSON/QR без расширения блока сводки.
- Сохранена поддержка H1Cloud HTTPS с самоподписанным сертификатом; строгая проверка включается через `H1CLOUD_TLS_VERIFY=1`.

# stage68-h1cloud-original-sub — 2026-06-18

- H1Cloud переведён на оригинальную публичную мастер-подписку `/sub/REMOTE_UUID`: тело ответа и Content-Type проксируются без декодирования, фильтрации, переименования, изменения `fp` и преобразования в JSON.
- Добавлена передача официальных форматов H1Cloud через `/sub/:slug/raw`, `/clash`, `/sing-box`, `/json` и `/page`; старые сохранённые URL с `/local` мигрируют автоматически.
- H1Cloud полностью исключён из JSON/HAPP-маршрутизации и из списка узлов на странице «Маршрутизация»; смешанный клиент получает H1Cloud через SUB, а 3x-ui — через JSON.
- Исправлена настройка «Показывать SUB»: в «Клиенты» теперь появляются компактные SUB + QR слева от JSON + QR только у клиентов с активным H1Cloud-узлом.
- Публичная страница подключения для H1Cloud использует оригинальную SUB как основной QR и URL импорта в приложения.
- Удалены устаревшие UI-поля фильтра транспортов и переписывания fingerprint для H1Cloud; Reality, XHTTP и XHTTP CDN передаются как опубликованы сервером.
- Для HTTPS H1Cloud поддержан штатный self-signed сертификат; строгую проверку можно включить переменной `H1CLOUD_TLS_VERIFY=1`.
- Cache-bust стилей обновлён до `v=168`.

# stage67-mobile-dashboard-online-clients-layout-fix

- Мобильный список онлайн-клиентов перестроен на устойчивую двухстрочную карточку: логин, комментарий и срок снова видны, кнопка «Подробнее» не обрезается, а быстрое продление остаётся отдельной нижней строкой.
- Убрано схлопывание карточек онлайн-клиентов в тонкие полосы: зафиксированы нормальная высота, автоматическая высота содержимого и внутренняя прокрутка списка.
- Четыре мобильных раздела Dashboard («Скачано», «По узлам», «Кто больше потратил», «Сроки») перенесены сразу под статистику и оформлены крупными плитками 2×2 в стиле верхних карточек.
- Заголовки раскрываемых отчётов на телефоне уменьшены и сделаны менее жирными; убрано дублирование большого внутреннего заголовка.
- Cache-bust стилей обновлён до `v=167`.

# stage66-mobile-dashboard-online-clients-polish

- В мобильном окне создания клиента поля «Комментарий», «Срок в днях», «Общий лимит» и «Лимит IP» выровнены по одинаковой ширине без смещения вправо.
- На мобильную главную добавлен полноценный переключатель разделов «Скачано», «По узлам», «Кто больше потратил» и «Сроки»; порядок синхронизируется с сохранённым порядком компьютерной версии.
- Заголовки мобильных отчётов уменьшены, чтобы длинные названия помещались аккуратнее.
- Мобильное окно онлайн-клиентов стало компактнее: логин, комментарий и срок видны сразу, подробности открываются отдельной кнопкой.
- В строку каждого онлайн-клиента добавлено быстрое продление: количество дней можно изменить, значение по умолчанию — 30.
- Список онлайн-клиентов получил собственную прокрутку, а кнопки обновления, автообновления и интервала стали меньше и ровнее.
- Cache-bust стилей обновлён до `v=166`, чтобы мобильный браузер сразу загрузил исправленную версию без старого CSS из кэша.

# stage65-mobile-modal-node-editor-polish

- Исправлена мобильная карточка узла: убрана пустая нижняя область, добавлена внутренняя прокрутка при необходимости, усилены кнопки закрытия, проверки, изменения и включения/отключения.
- Исправлено мобильное окно создания и редактирования клиента: окно снова прокручивается, список узлов остаётся внутри него, кнопки закрытия и сохранения доступны.
- На мобильной странице клиентов кнопка «Импорт и синхронизация» теперь занимает ту же ширину, что и остальные основные действия.
- Из названий серверов в клиентской подписке убраны суффиксы «XHTTP» и «XHTTP CDN»; транспорт по-прежнему виден отдельной строкой приложения.
- Страница редактирования узла собрана в две колонки: «Что меняется» перенесено вправо и закреплено, удаление узла перенесено в нижнюю панель действий.
- Смягчено выделение выбранного узла: яркая синяя рамка больше не отвлекает от списка.

# stage64-h1cloud-transports-filter — 2026-06-17

- Фильтр H1Cloud теперь выбирает Reality, XHTTP и XHTTP CDN независимо; добавлена команда «Выбрать все».
- Фильтрация применяется к фактическим параметрам каждой VLESS-ссылки в `/sub/UUID/local`, поэтому в общей подписке остаются только выбранные транспорты.
- Названия ссылок приведены к понятному виду: основной узел для Reality, суффиксы `· XHTTP` и `· XHTTP CDN` без числовых окончаний.
- В редактор H1Cloud-узла добавлена панель управления XHTTP, XHTTP CDN и Reality с полями из официальных команд H1Cloud и готовыми командами для копирования.
- XHTTP CDN применяется через H1Cloud API; Reality безопасно применяется только при наличии поддерживаемого API endpoint, иначе выводится точная консольная команда без прямого вмешательства в файлы H1Cloud.
- Добавлены миграции БД для нового многозначного фильтра и сохранения параметров транспортов. Старые режимы фильтра конвертируются автоматически.
- Добавлена адаптивная компоновка новых настроек для мобильной версии.

## stage63-delete-modal-tabs-dirty-fix

- Полное удаление клиента на 3x-ui v3 переведено с `detach` на `POST /panel/api/clients/del/:email`; добавлены совместимые резервные способы, удаление из legacy inbound и проверка глобального списка клиентов после команды.
- H1Cloud удаляется через `DELETE /clients/NAME` с резервным `POST /delete`, после чего агрегатор проверяет, что клиент действительно исчез.
- При ошибке хотя бы одного удалённого узла локальная запись клиента больше не стирается: панель показывает ошибку и оставляет клиента для повторной попытки.
- Кнопки «Выбрать все» и «Снять все» получили прямые обработчики и реально переключают все доступные чекбоксы узлов при создании и редактировании клиента.
- Окно создания клиента помещается в высоту браузера: общая прокрутка страницы заблокирована, прокручивается только список узлов, кнопка создания остаётся видимой.
- Сохранённый порядок вкладок применяется до показа основной области, поэтому Dashboard и другие переставляемые вкладки больше не мигают старой первой вкладкой.
- Плавающая панель «Сохранить / Отменить» теперь реагирует только на реальные изменения полей пользователем, а не на переключение вкладок или клики по свободной области.
- Обновлён cache-bust CSS до `v=163`.

## stage62-h1cloud-client-adopt-mobile-sheet

- H1Cloud-клиенты теперь надёжнее привязываются к локальному клиенту агрегатора: если имя уже есть на H1Cloud, агрегатор забирает существующий UUID через `/clients/NAME`, `/info?name=NAME` или список клиентов вместо ошибки `user_already_exists/email already in use`.
- При создании/обновлении H1Cloud клиента агрегатор передаёт лимит трафика и лимит устройств, если они заданы в карточке клиента.
- Улучшены кнопки «Выбрать все» и «Снять все» в редакторе клиента: они выровнены, меняют все чекбоксы узлов, показывают активное состояние и вызывают события изменения формы.
- Мобильная страница узлов снова открывает детали выбранного узла как нижнюю карточку с действиями, а не вставляет длинную панель под список.
- Экран входа переведён в текущую Dark Slate тему; обновлён cache-bust CSS до `v=162`.

## stage61-mobile-ui-repair

- Исправлена мобильная главная: карточка «Сколько было скачано» получила более компактный сильный шрифт и новый значок.
- Переделана мобильная страница узлов: поиск, фильтры «Тип/Статус» и кнопка обновления выровнены; карточки узлов больше не перекрывают текст, флаги, статусы и транспорт.
- Исправлен белый фон у выпадающих списков: select/option теперь наследуют тёмную тему панели.
- Переделана мобильная страница клиентов: верхний поиск отделён от действий, кнопки не накладываются, массовое удаление помещается в экран.
- Исправлен desktop-поиск клиентов: «Найти» и «Сбросить» стоят в одной строке справа от поля.
- Добавлены кнопки «Выбрать все» и «Снять все» для узлов при создании и редактировании клиента.

## stage56-dark-slate-real-fix

- Исправлено фактическое скрытие логина/пароля при выборе `API Token` в добавлении и редактировании узла: старые CSS-правила больше не перебивают `.hidden`.
- Пагинация узлов усилена через JS и CSS: лишние строки получают принудительное `display:none`, поэтому 11-й узел уходит на страницу 2 при лимите 10.
- Правая панель деталей узла снова показывает только выбранный узел; скрытые карточки не растягивают страницу.
- Редактирование порядка больше не вставляет некрасивые панели сверху и в сайдбар: теперь одна кнопка «Настроить порядок» над пользователем открывает модальное окно с сохранением и сбросом.
- Таблица клиентов уплотнена и зафиксирована по ширине: статус, JSON и QR остаются справа внутри страницы, комментарии обрезаются без переноса.
- Блок пользователя и выход в сайдбаре подняты выше и не прижаты к самому низу.
- Дополнительно уплотнены узлы, диагностика и поля авторизации в форме узла.
- Обновлён кэш CSS до `v=156`.


## stage55-dark-slate-polish

- Исправлено скрытие логина/пароля при выборе метода API Token в добавлении и редактировании узла.
- Страница узлов дополнительно расширена: список и правая панель стали шире, пагинация снова скрывает лишние узлы на странице 2.
- Правая панель деталей узла показывает только выбранный узел, длинные URL/transport/fingerprint обрезаются без раздувания страницы.
- Блок пользователя и кнопка выхода в сайдбаре подняты выше и сделаны компактнее.
- Добавлена локальная настройка порядка кнопок: можно включить режим редактирования и перетаскивать кнопки левого меню, вкладки dashboard/nodes/settings; есть возврат по умолчанию.
- В dashboard добавлена сортировка списка истекающих клиентов: меньше дней, больше дней, сначала истёкшие.
- Онлайн-клиенты в модальном окне стали компактнее: используемые узлы спрятаны в подробности, окно уже и выше.
- Подсказка Happ Control App снова следует за прокруткой как sticky-панель и получила цветовую схему Dark Slate.



## stage54-dark-slate-compact-clients-nodes-fix

- Поднят блок пользователя и кнопка выхода в боковой панели, чтобы не провоцировать лишнюю прокрутку.
- Уплотнена страница узлов: шире область списка и деталей, компактнее строки, фильтры и правая панель.
- Форма добавления узла стала короче: API Token/логин и Inbound выравниваются в сетке, логин/пароль скрыты для режима API Token.
- Диагностика получила широкий компактный селект узла и связанную кнопку проверки.
- Таблица клиентов упрощена: чекбокс, ID, клиент, комментарий, статус и JSON/QR; подробности и редактор открываются по нажатию на логин.
- Мобильные фильтры клиентов приведены к компактной сетке 2x2 в тёмно-сером стиле.

## stage53-dark-slate-polish-layout-fix

- Исправлено критическое отображение всех карточек деталей узлов сразу: скрытые `node-detail-card` снова реально скрываются.
- Страница «Узлы»: список остаётся первой вкладкой, фильтры стали шире, строка и панель деталей уплотнены, длинные значения обрезаются без переноса.
- Форма «Добавить узел» на ПК переведена в двухколоночную компоновку, чтобы страница стала короче и не занимала лишнюю высоту.
- Сайдбар стал компактнее: блок пользователя и кнопка выхода подняты выше, меню получило более тёмно-серую, а не синюю подложку.
- Главная страница дополнительно уплотнена, чтобы убрать лишнюю вертикальную прокрутку при обычной высоте браузера.
- Клиенты и таблицы дополнительно уменьшены на один шаг: кнопки, бейджи, строки и действия стали компактнее.
- Обновлён кэш CSS до `v=153`.

## stage51-deep-dark-slate-glass-system-redesign

- Сделан глубокий редизайн системы интерфейса, а не только наложение цветов поверх старой темы.
- Усилены CSS-override правила для карточек, таблиц, форм, кнопок, модалок, вкладок, бейджей, плавающего сохранения и мобильного нижнего меню.
- Страница узлов по умолчанию открывает список, добавлены более стабильные SVG-флаги рядом с названиями регионов.
- ПК-версия: Dark Glass цвета + компактная Dark Slate структура + пользователь и выход внизу сайдбара.
- Мобильная версия приведена к той же палитре, что ПК: компактные карточки узлов, нижняя панель деталей, без старых ярко-синих крупных блоков.
- Добавлены SVG-флаги для Латвии и Турции, чтобы не было пустых квадратов вместо emoji-флагов на Windows/Android.



## stage50-dark-slate-glass-forced-redesign

- Редизайн вынесен в отдельный файл `public/css/redesign.css`, который подключается после основного CSS, чтобы изменения точно перекрывали старую тему.
- Добавлен явный маркер `data-redesign="stage50-dark-slate-glass"` и класс `redesign-slate-pro` на body.
- Цвета: Dark Glass / graphite / blue, структура: компактный Dark Slate, флаги у узлов и блок пользователя в сайдбаре.
- Мобильная версия использует ту же цветовую палитру, что и ПК, без старых ярко-синих крупных блоков.



## stage49-dark-slate-pro-redesign

- Объединён выбранный редизайн: цвета Dark Glass, минимальный layout Dark Slate и элементы Server Console Pro.
- Добавлен блок пользователя внизу бокового меню рядом с кнопкой выхода.
- Флаги стран сохранены рядом с названиями узлов на ПК и мобильной версии.
- Мобильная версия получила те же тёмные цвета, что и ПК, компактные карточки узлов и нижнюю панель деталей.
- Обновлены стили таблиц, карточек, кнопок, бейджей и панели деталей для единого вида.
## stage48-mobile-ui-polish-save-cancel

- Mobile-only cleanup: duplicate top tab buttons are hidden on dashboard/nodes because mobile uses section cards.
- Node list cards are compact on phones: no oversized blue row, smaller badges and inline up/down order buttons.
- Routing mobile layout is tighter, routing node cards keep flag + name, and GeoSite/GeoIP action rows no longer overflow horizontally.
- Global floating save bar now shows both “Отменить изменения” and “Сохранить”, and appears after routing quick-add actions too.

## stage47-mobile-layout-repair

- Исправлена мобильная версия страницы «Узлы» после нового desktop-layout: строки узлов снова выглядят как компактные карточки, не растягиваются синим блоком и не вылезают за экран.
- Правая панель деталей узла на телефоне теперь не занимает место в списке: открывается отдельной нижней карточкой после нажатия на узел и закрывается кнопкой ×.
- Исправлены мобильные кнопки порядка узлов ↑/↓, бейджи транспорта, статуса и активности: всё помещается в ширину телефона.
- Исправлена мобильная таблица клиентов после объединения «Имя / логин»: отдельная колонка логина не ожидается, карточки клиентов не ломают сетку.
- Добавлены защитные мобильные правки для форм, вкладок настроек, плавающей кнопки сохранения и широких служебных блоков, чтобы страницы не создавали горизонтальную прокрутку.

## stage46-compact-pages-pagination-floating-save

- В подписках Happ убрано слово `REALITY` из стандартного serverDescription: новый шаблон по умолчанию — `VLESS / {network} / JSON`, старый точный шаблон мигрируется автоматически.
- Для H1Cloud-ссылок убрано автоматическое добавление суффикса `Reality` в название сервера; `WS` и нумерация дублей сохраняются.
- Страница «Узлы» стала компактнее: скрыт верхний заголовочный блок, список поднимается выше, добавлена пагинация узлов и настройка количества строк на странице: 10, 12 или 15.
- В настройках панели добавлен пункт «Узлов на странице». По умолчанию — 10, лишние узлы уходят на следующие страницы.
- В формах добавления узла по умолчанию выбран `API Token`; для редактирования сохранён старый метод у уже существующих узлов, но новый дефолт — token.
- Список стран/регионов теперь отдаётся в формы отсортированным по русскому алфавиту, а «Другое / свой регион» уходит в конец.
- Справочник GeoSite / GeoIP в маршрутизации растянут на всю ширину страницы.
- В настройках вкладки раскладываются до 8 пунктов в одной строке.
- Добавлена единая плавающая кнопка сохранения: появляется только когда форма реально изменилась, исчезает при возврате к исходным значениям и отправляет активную изменённую форму.
- В списке клиентов объединены колонки «Имя» и «Логин»: убрана дублирующая колонка, кнопка имени получила фиксированную ширину, таблица стала компактнее.

## stage44-3xui-inbound-utls-select

- В редакторе обычного 3x-ui узла поле `uTLS fingerprint` заменено на выпадающий список: не менять, firefox, edge, android, ios, random, randomized, safari, chrome, 360, qq и ручной режим.
- При выборе ручного режима появляется отдельное поле для собственного uTLS/Xray значения, например `HelloRandomizedNoALPN` или `HelloChrome_106_Shuffle`.
- Если стоит галочка «Сохранить эти параметры в 3x-ui», выбранный fingerprint теперь отправляется в оригинальную 3x-ui панель и записывается в `realitySettings.fingerprint` или `tlsSettings.fingerprint`.
- Кнопка «Установить рекомендуемые значения» теперь выставляет fingerprint через новый select, а для VLESS/Trojan/Hysteria предлагает `firefox` вместо старого `chrome`.

## stage43-h1cloud-reality-fp-filter

- Для H1Cloud узлов добавлены новые поля в базе `nodes`: `h1cloud_link_mode` и `h1cloud_fingerprint`.
- В форме добавления и редактирования H1Cloud появился фильтр ссылок: только VLESS REALITY, только VLESS, только VLESS WS или все ссылки из H1Cloud. По умолчанию используется только VLESS REALITY.
- Добавлен выбор uTLS browser/fingerprint для H1Cloud ссылок: не менять, firefox, edge, android, ios, random, randomized, safari, chrome, 360, qq и ручной режим для uTLS Hello... значений.
- При сборке общей подписки агрегатор фильтрует строки из `/sub/UUID/local`, оставляет выбранный тип ссылок и при необходимости заменяет/добавляет параметр `fp=...`, сохраняя остальные параметры VLESS/REALITY: `pbk`, `sni`, `sid`, `spx`, `flow`, адрес и порт.
- Обновлены README и `docs/H1CLOUD_NODES.md` с описанием фильтра, fingerprint и ограничений ручных значений.

## stage42-h1cloud-vless-nodes

- Добавлен новый тип узла **H1Cloud VLESS** рядом с 3x-ui.
- Агрегатор теперь умеет проверять H1Cloud API, импортировать клиентов через `/clients`, создавать клиентов через `/create`, продлевать/переименовывать через `/edit`, удалять через `/clients/:name`, а также вызывать ban/unban при отключении клиента.
- В общую подписку добавляются готовые ссылки H1Cloud из `/sub/REMOTE_UUID/local`; ссылки переименовываются под название узла агрегатора и не требуют 3x-ui inbound.
- На вкладках «Узлы», «Клиенты», «Маршрутизация» и «Перенаправление» добавлены подписи для H1Cloud вместо 3x-ui Inbound ID.
- Добавлена инструкция `docs/H1CLOUD_NODES.md` с настройкой портов, API URL, SUB URL и ограничениями H1Cloud API.

## stage41-happ-control-free-flags-icons

- Добавлена вкладка/блок «Happ Control App» в настройках: бесплатные параметры Happ-подписки, support-url, announce/sub-info, serverDescription, JSON/Xray, routing/geosite/geoip и DNS собраны в одном месте.
- Добавлена правая справочная панель: при наведении или фокусе на параметре показывается описание, что он делает.
- Платные Happ app-control параметры не выводятся и не отправляются: Provider ID, autoconnect, ping-on-open/TCP ping, update-on-open, fragmentation/noises, Happ mux и no-limit остаются отключёнными.
- Флаги узлов усилены через единый emoji fallback: если country_flag пустой, флаг берётся по country_code или названию региона.
- Готовые сервисы маршрутизации получили значки и domain-fallback правила рядом с geosite, чтобы iOS/Happ стабильнее обрабатывал WhatsApp/Meta/YouTube/Telegram/OpenAI.
- Loyalsoldier GeoSite/GeoIP оставлен основным источником по умолчанию, добавлена быстрая подстановка Russia rules для справки и кастомных тестов.

## stage40-free-geodata-adblock-routing

- Полностью исключены платные Happ app-control параметры из интерфейса и генерации подписок; остаются только бесплатные параметры подписки: title, userinfo, support-url/announce/sub-info и автообновление.
- Во вкладке «Маршрутизация» добавлен блок выбора GeoData источника: официальный/локальный источник клиента, Loyalsoldier как основной источник или свои URL для будущих собственных geosite.dat/geoip.dat.
- Happ routing-профиль оставлен только как опциональная бесплатная галка и выключен по умолчанию; при включении в него попадают выбранные GeoData URL, LastUpdated, VPN/direct/block правила.
- Добавлена блокировка рекламы: отдельные редактируемые списки domain/geosite и ip/geoip, правило ставится первым и отправляет совпадения в outbound `block`. По умолчанию используется `geosite:category-ads-all`.
- Новый дефолт маршрутизации при включении: режим «всё через proxy, кроме...», `geosite:category-ru` и `geoip:ru` идут direct, остальное через VPN.

## stage39-free-geodata-source-selector

- Happ Provider ID / платные app-control параметры полностью отключены: агрегатор не отправляет providerid, fragmentation/noises/mux/no-limit/app-control заголовки.
- Happ routing-профиль теперь опциональный и выключен по умолчанию; старое автоматическое включение из stage38 не применяется, пока владелец явно не включит его во вкладке «Маршрутизация».
- Добавлен выбор источника GeoData: официальный/локальный источник клиента, Loyalsoldier по умолчанию или свои URL.
- Loyalsoldier GeoSite/GeoIP остаётся доступным как основной список для тегов geosite:/geoip:, но можно быстро переключиться на официальный режим или кастомные ссылки.
- Автообновление подписки отделено от Happ Provider ID: profile-update-interval/subscription-auto-update-enable отправляются как обычные бесплатные заголовки.


## stage38-happ-routing-geodata-autoupdate

- Добавлен Happ routing-профиль в подписку через HTTP header `routing` и строку `happ://routing/onadd/...` в SUB/Happ body.
- GeoIP/GeoSite по умолчанию — Loyalsoldier URL: `geoip.dat` и `geosite.dat`.
- В routing-профиль попадают `ProxySites`, `ProxyIp`, `DirectSites`, `DirectIp`, `DomainStrategy=IPIfNonMatch`, `UseChunkFiles=true`.
- `LastUpdated` берётся из ревизии подписки; сохранение маршрутизации поднимает ревизию и заставляет Happ перекачать geofile.
- Добавлен `/happ-routing-json/:slug` для проверки профиля маршрутизации.

## stage37-geodata-catalog-search

- Добавлен компактный справочник GeoSite / GeoIP во вкладку «Маршрутизация»: поиск, прокрутка, копирование строки, быстрые кнопки «VPN» и «DIRECT».
- Справочник автоматически пытается подтянуть актуальные geosite-категории из v2fly/domain-list-community и дополняет их fallback-списком Happ/Loyalsoldier.
- GeoIP-справочник содержит ISO alpha-2 страны/регионы и дополнительные категории Loyalsoldier: private, telegram, facebook, google, cloudflare, netflix, twitter, tor и др.
- DAT-ссылки Happ/Loyalsoldier остаются источником правил на стороне клиента; агрегатор передаёт в JSON обычные `geosite:` / `geoip:` теги.


## stage36-happ-geodata-online-flags-polish

- Добавлен Happ/Loyalsoldier как основной geodata-профиль для маршрутизации JSON: geosite.dat/geoip.dat сохраняются в агрегаторе как источник правил, а в JSON остаются обычные `geosite:`/`geoip:` теги.
- Пресет Mail.ru/My.ru/VK расширен `geosite:mailru-group` и остаётся выше RU-direct правил, чтобы Mail/My/VK можно было вести через VPN при общем `geoip:ru`/`geosite:category-ru` напрямую.
- Переработано окно онлайн-клиентов на desktop: компактные строки с логином, комментарием, сроком, количеством узлов, флагами и кнопкой «Подробнее».
- Переработана карточка подробностей онлайн-клиента/быстрая карточка: компактная сетка, аккуратные узлы и действия без визуального развала.
- Флаги узлов теперь выводятся как emoji fallback во всех основных местах: создание/редактирование клиента, детали, онлайн-клиенты, узлы, маршрутизация и перенаправления.

## stage35-routing-priority-loyalsoldier-mailru

- Добавлен приоритет маршрутизации для режима «всё через proxy, кроме...»: выбранные сервисы и дополнительные domain/geosite теперь идут через VPN перед широкими direct-правилами RU.
- Добавлен готовый сервис «Mail.ru / My.ru / VK Group» для сценария Mail.ru/My.ru → VPN, а geosite:category-ru/geoip:ru → direct.
- Подсказки в разделе маршрутизации объясняют использование Loyalsoldier/Happ geosite.dat и geoip.dat.
- Добавлена валидация ext:geosite_alias.dat:tag и ext:geoip_alias.dat:tag для кастомных .dat файлов Xray/3x-ui.


## stage34-ios-geosite-geoip-routing-restore

- Вернул сохранение `geosite:` и `geoip:` в iOS-safe JSON.
- Исправил `domainStrategy`: вместо `AsIs` снова используется `IPIfNonMatch`, чтобы правила `geoip:ru` работали для доменов после DNS-резолва.
- iOS-safe режим теперь чистит только RAW/null/битые поля, но не ломает геомаршрутизацию.

# Changelog

## stage32-ios-subscription-no-redirect
- Public /sub, /json and /happ links now return content directly with HTTP 200 instead of redirecting to ?rev=.
- This fixes iOS/CLI clients that do not follow 302 redirects for subscriptions.

# Changelog

## stage31-centered-operation-screen
- Обновлён экран долгих операций: теперь он центрирован по экрану, выглядит как отдельное красивое окно со спиннером, статусом и прогресс-индикатором.
- После завершения операции панель показывает короткий статус и автоматически возвращает пользователя обратно.

# stage30-saved-api-token-validation-fix

- Исправлена HTML5-проверка формы редактирования узла: если API Token уже сохранён, поле больше не требует повторного ввода.
- При изменении мелких настроек узла сохранённый токен продолжает использоваться на сервере.

# Changelog

Изменения ведутся по мере обновлений проекта. Подробное описание конкретной тестовой сборки обычно передаётся отдельно вместе с архивом.


## stage52-dark-slate-minimal-repair

- Пересобран редизайн без агрессивных правил stage51.
- Цветовая схема приведена к варианту 3 Dark Slate Minimal.
- Исправлены раздутые чекбоксы, switch-row, preset-card, check-card и формы.
- Уменьшены шрифты, кнопки, бейджи и отступы для таблиц и карточек.
- На странице узлов первым открыт «Список узлов», «Добавить узел» перенесён вторым.
- Укорочены транспортные бейджи в списке узлов.
- Панель клиентов сделана компактнее, поиск перенесён ближе к блоку «Все клиенты».
- Мобильная версия приведена к тем же цветам и компактным размерам.


## stage59-dark-slate-cleanup

- Исправлено: панель «Есть несохранённые изменения» появляется только после реального изменения полей, а не при клике по вкладкам или пустой области.
- Доработано окно «Настроить порядок»: кнопки вверх/вниз, сохранить и сброс работают через прямые обработчики.
- Уплотнён двухколоночный список клиентов: ID, статус, JSON и QR больше не уезжают за границы.
- Возвращён более светлый тёмно-серый фон левого меню.
- Добавлены мобильные правки для вкладок клиентов в стиле Dark Slate.

## stage60-dark-slate-hardening

- Обновлён экран входа под общий Dark Slate стиль.
- Добавлены настройки безопасности: автовыход при бездействии и выход при смене IP.
- Исправлена строгая логика плавающей панели «Сохранить / Отменить»: она не должна появляться от простого клика по вкладкам или пустой области.
- Доработана страница узлов: разнесены drag-handle и ID, список растянут вниз без наложения пагинации, поиск и фильтры выровнены по зонам списка/деталей.
- Исправлена активация первой вкладки после пользовательской сортировки порядка вкладок dashboard/settings.
- Уплотнён двухколоночный список клиентов: ID отделён от чекбокса, статус укорочен, JSON и QR остаются внутри строки.
- Усилены обработчики окна «Настроить порядок».

## stage74-h1cloud-sub-json-happ-ui-fix

- Исправлена вёрстка блока H1Cloud 3x-ui на форме добавления узла: чекбоксы общего трафика и локального срока больше не сжимаются в узкую колонку.
- Для H1Cloud 3x-ui готовые ссылки провайдера из SUB-порта сохраняются без переписывания XHTTP `extra`, чтобы не ломать импорт в Happ, если прямая ссылка провайдера работает.
- В быстрой карточке клиента при наличии H1Cloud 3x-ui основной кнопкой стала `SUB / Happ`; JSON оставлен вторым, так как сложный XHTTP CDN провайдера надёжнее импортируется Happ именно как VLESS/SUB, а не как пересобранный Xray JSON.
- Повышена версия CSS-кеша.
## 2.5.0 — traffic, updater and responsive control

- Исправлено веб-обновление на системах без `unzip`: установщик добавляет пакет, а updater умеет распаковывать ZIP через `python3 -m zipfile`.
- Учёт 3x-ui inbound дополнен суммой `clientStats`, поэтому LTE/xHTTP больше не остаётся на `0 ГБ`, когда агрегат `up/down` панели равен нулю.
- Интервалы автообновления узлов и онлайн-клиентов перенесены в настройки: 10/30/60 секунд или ручной режим.
- Уплотнены и выровнены редактор клиента, действия узла, фильтры, онлайн-список, резервные копии и экран обновления на компьютере и телефоне.
- В маршрутизацию добавлен источник runetfreedom Russia rules и российские категории `ru-blocked`, `ru-blocked-community`, `ru-whitelist`.
- README оставляет только актуальные возможности, установку и диагностику; подробная история хранится в этом файле.
