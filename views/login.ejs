<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="csrf-token" content="<%= csrfToken %>">
  <meta name="theme-color" content="#070b16">
  <title>Вход — Nexus Panel</title>
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" type="image/png" sizes="192x192" href="/img/nexus-logo-192.png">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <link rel="manifest" href="/site.webmanifest?v=240">
  <script>
  (function(){
    try {
      const saved = localStorage.getItem('agg.color-theme.v1');
      const mode = saved === 'light' ? 'light' : 'dark';
      document.documentElement.dataset.colorMode = mode;
      document.documentElement.dataset.colorTheme = mode;
    } catch (_) {
      document.documentElement.dataset.colorMode = 'dark';
      document.documentElement.dataset.colorTheme = 'dark';
    }
  })();
  </script>
  <link rel="stylesheet" href="/css/spectrum-clear.css?v=270" />
  <script src="/js/security.js?v=1"></script>
  <% if (typeof isDevelopment !== 'undefined' && isDevelopment) { %><script src="/js/dev-inspector.js?v=1" defer></script><% } %>
</head>
<body class="nexus-spectrum login-page">
  <main class="login-wrap">
    <section class="card login-card">
      <div class="login-brand"><img class="login-brand-logo" src="/img/nexus-logo-192.png" alt="Логотип Nexus Panel">Nexus <span>Panel</span><small>Spectrum Clear</small></div>
      <h1>Вход в панель</h1>

      <% if (error) { %>
        <div class="alert err"><%= error %></div>
      <% } %>

      <form method="post" action="/login" class="login-form">
        <label>Логин</label>
        <input name="username" required autocomplete="username" />

        <label>Пароль</label>
        <input name="password" type="password" required autocomplete="current-password" />

        <button type="submit" class="login-submit">Войти</button>
      </form>
    </section>
  </main>
</body>
</html>
